# Dataset Configs

Each YAML file describes one reusable deconvolution input dataset. The current configs register only datasets with local input files present in this workspace.

The full paper benchmark names from the old SEML configs, including missing Brain simulations and additional Heart simulations, should be added here after their `h5mu` inputs are restored or downloaded.
