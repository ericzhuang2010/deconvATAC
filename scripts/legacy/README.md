# Legacy Scripts

These scripts were moved from the repository root during cleanup. They are retained for reference only.

Use the maintained replacements from the top-level `README.md`:

- `scripts/run_deconvolution.py` for Cell2location, RCTD, Tangram, DestVI, SpatialDWLS, and `nnls`.
- `scripts/regenerate_heart_simulations.py` for Heart simulation regeneration.
- `scripts/evaluate_runs.py` for standardized run evaluation.
